services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: flex-brain-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333" # REST API
      - "6334:6334" # gRPC API
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
  postgres_data:
